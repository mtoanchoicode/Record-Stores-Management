extends ../../layouts/default.pug
include ../../mixins/alert.pug

block main 
    +alert-error(5000)

    .container.my-5 
        .row.justify-content-center 
            .col-lg-8 
                h1.text-center Enter OTP verification code
                form(
                    action="/user/password/otp"
                    method="POST"
                )
                    .mb-3
                        label.form-label(for="email") Email
                        input.form-control(
                            type="email"
                            id="email"
                            name="email"
                            value= email
                            required
                            readonly
                        )
                    .mb-3
                        label.form-label(for="otp") OTP verification code
                        input.form-control(
                            type="text"
                            id="otp"
                            name="otp"
                            required
                        )
                    .d-grid.mb-3
                        button.btn.btn-primary.btn-block(
                            type="submit"
                        ) Confirm
